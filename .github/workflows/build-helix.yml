name: Build Helix
on:
  push:
    paths:
      - 'repo/Containerfile.helix'
  workflow_dispatch:
jobs:
  call-reusable-build:
    uses: ./.github/workflows/reusable-build.yml
    with:
      name: 'helix'